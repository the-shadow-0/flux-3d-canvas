import { FullscreenLayout } from '@/components/layout/fullscreen-layout';

export default function HomePage() {
  return (
    <FullscreenLayout>
      {/* SceneCanvas is rendered by default in FullscreenLayout */}
      {/* Additional page-specific content could go here if needed */}
    </FullscreenLayout>
  );
}
